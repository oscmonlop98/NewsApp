# NewsApp

This project was generated with Angular CLI version 11.0.7 and Node version 14.15.4.

## Frontend app

Run `ng serve` for start the app. Navigate to `http://localhost:4200/`.

## Backend server

Launch the server with a Node.js Debugger, and will run at http://localhost:3000 .
